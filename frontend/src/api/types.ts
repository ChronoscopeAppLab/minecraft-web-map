export type InitialState = {
  prefix: string;
};

export type Spot = {
  id: number;
  type: number;
  x: number;
  z: number;
  name: string;
  hira: string;
  color: string;
  detail: string;
  image: string;
};
